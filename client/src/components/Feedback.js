import { useState } from "react";

const Feedback = ({ isCorrect, customFunction }) => {
    return (
        <>
            <h1>Feedback test</h1>
            {/* <h3 name={feedback}>{feedback}</h3>
            <button onClick={customFeedback}>Check</button> */}
        </>
    );

};

export default Feedback;